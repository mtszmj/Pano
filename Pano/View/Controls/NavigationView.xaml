<UserControl x:Class="Pano.View.Controls.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Pano.View.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="800"
             d:DesignStyle="{StaticResource DesignTimeBackgroundStyle}"
             DataContext="{Binding Navigation, Source={StaticResource Locator}}">
    <WrapPanel>
        <Label Content="Strona:" Margin="5 5 0 5"/>
        <Label Content="{Binding CurrentPage, Delay=1000}" Margin="5" MinWidth="100"/>
        <Button Content="Projekty" Margin="5" MinHeight="20" MinWidth="80" Command="{Binding NavigateToProjects}"/>
        <Button Content="Panoramy" Margin="5" MinHeight="20" MinWidth="80" Command="{Binding NavigateToPanorama}"/>
    </WrapPanel>
</UserControl>
