<UserControl x:Class="Pano.View.Control.ProjectNewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Pano.View.Control"
             xmlns:view="clr-namespace:Pano.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DesignStyle="{StaticResource DesignTimeBackgroundStyle}"
             DataContext="{Binding ProjectNew, Source={StaticResource Locator}}">
    <StackPanel>
        <Label HorizontalContentAlignment="Center" Margin="0 5 0 5" FontSize="20" FontWeight="Bold" BorderThickness="1" BorderBrush="Black">Nowy Projekt</Label>
        <local:ProjectEditDetailsView DataContext="{Binding Path=SelectedProject, Mode=TwoWay}"/>
        <Button Content="Dalej" Command="{Binding CreateSelectedProjectCommand}" 
                Style="{StaticResource DefaultButtonStyle}"/>
    </StackPanel>
</UserControl>
